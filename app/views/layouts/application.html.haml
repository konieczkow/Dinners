!!!
%html{:lang => "en"}
  %head
    %meta{:charset => "utf-8"}
      %meta{:content => "width=device-width, initial-scale=1.0", :name => "viewport"}
        %title= content_for?(:title) ? yield(:title) : "Dinners"
        = csrf_meta_tags
        / Le HTML5 shim, for IE6-8 support of HTML elements
        /[if lt IE 9]
          <script src="http://html5shim.googlecode.com/svn/trunk/html5.js" type="text/javascript"></script>
        = stylesheet_link_tag "application", :media => "all"
  %body{:class => "#{controller_name} #{action_name}", :id => "#{controller_name}-#{action_name}"}
    .container-fluid
      %header
        %h1 Dinners
        %h2 Can't decide what to eat? Let's vote!

      .row-fluid
        .span12.offset1
          = yield

      %footer
        %p= link_to 'Mobile version', 'http://m.dinners.heroku.com/'

    = javascript_include_tag "application"